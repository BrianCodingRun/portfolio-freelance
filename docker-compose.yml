services:
  app:
    build: .
    container_name: portfolio_app_new
    restart: unless-stopped
    ports:
      - "3001:3000" # hôte:container — le container écoute toujours sur 3000 en interne
    env_file:
      - .env.production
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--no-verbose",
          "--tries=1",
          "--spider",
          "http://localhost:3000",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
